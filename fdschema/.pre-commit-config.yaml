repos:
  - repo: local
    hooks:
    - id: pytest-check
      name: pytest-check
      stages: [pre-commit]
      types: [python]
      entry: pytest
      language: system
      pass_filenames: false
      always_run: true
